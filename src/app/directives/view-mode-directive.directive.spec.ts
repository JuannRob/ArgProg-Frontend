import { ViewModeDirectiveDirective } from './view-mode-directive.directive';

describe('ViewModeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewModeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
